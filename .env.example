# Copy to .env.local and fill in from your Supabase project settings (Project Settings → API).
# Leave unset to run in local-only mode (no sign-in, no cloud sync) — the app works fine without these.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
